# Set the default registry for the scope
@vennex:registry=https://gitlab.com/api/v4/projects/${CI_PROJECT_ID}/packages/npm/

# URL to your GitLab instance with a placeholder for the token
//gitlab.com/api/v4/projects/${CI_PROJECT_ID}/packages/npm/:_authToken=${CI_JOB_TOKEN}
//gitlab.com/api/v4/packages/npm/:_authToken=${CI_JOB_TOKEN}

# For local development, you'll need to replace the variables with actual values
# or use a personal access token with the api scope
# //gitlab.com/api/v4/projects/YOUR_PROJECT_ID/packages/npm/:_authToken=YOUR_TOKEN
# //gitlab.com/api/v4/packages/npm/:_authToken=YOUR_TOKEN